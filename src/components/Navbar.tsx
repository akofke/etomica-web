import * as React from "react";

export const Navbar = () => {
    return(
        <nav className="pt-navbar">
            <div className="pt-navbar-group pt-align-left">Etomica</div>
        </nav>
    );
};
